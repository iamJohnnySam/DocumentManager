<Window x:Class="DocumentManager.Views.RevisionNotesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revision Notes" Height="260" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter notes for this revision:" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" x:Name="NotesBox" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" Padding="4"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="New Revision" Width="110" Click="NewRevision_Click" Margin="0,0,8,0"
                    ToolTip="Save as a new revision (increments version number)"/>
            <Button Content="Save In Place" Width="110" Click="SaveInPlace_Click" Margin="0,0,8,0"
                    ToolTip="Overwrite the current revision without incrementing"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
