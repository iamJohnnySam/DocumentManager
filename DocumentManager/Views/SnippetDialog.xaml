<Window x:Class="DocumentManager.Views.SnippetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Snippet" Height="360" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Save LaTeX Snippet" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="Snippet Name:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="2" x:Name="NameBox" Margin="0,0,0,8" Padding="4"/>

        <TextBlock Grid.Row="3" Text="Content:" Margin="0,0,0,4" VerticalAlignment="Top"/>
        <TextBox Grid.Row="3" x:Name="ContentBox" Margin="0,24,0,0" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Padding="4"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" Width="80" IsDefault="True" Click="Save_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
