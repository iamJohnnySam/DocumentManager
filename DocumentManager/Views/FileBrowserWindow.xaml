<Window x:Class="DocumentManager.Views.FileBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Browse Shared Sections" Height="520" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Browse shared LaTeX sections"
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,8">
            Select sections to insert \input references at the cursor position in the current file.
        </TextBlock>

        <DataGrid Grid.Row="2" x:Name="SectionsGrid" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                        Width="60"/>
                <DataGridTextColumn Header="Section Name" Binding="{Binding Name}" Width="200"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Latest Rev" Binding="{Binding LatestRevisionDisplay}" Width="80"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Latest Notes" Binding="{Binding LatestNotes}" Width="*"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Insert Selected" Width="110" IsDefault="True" Click="Apply_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="90" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
