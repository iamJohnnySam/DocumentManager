<Window x:Class="DocumentManager.Views.FileBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Available Shared Sections" Height="520" Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Browse shared LaTeX sections available for inclusion"
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,8">
            Select sections to include in your project. Already-included sections are checked.
        </TextBlock>

        <DataGrid Grid.Row="2" x:Name="SectionsGrid" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Include" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                        Width="60"/>
                <DataGridTextColumn Header="Section Name" Binding="{Binding Name}" Width="160"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Latest Rev" Binding="{Binding LatestRevisionDisplay}" Width="80"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Pinned Rev" Binding="{Binding PinnedRevisionDisplay}" Width="80"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Latest Notes" Binding="{Binding LatestNotes}" Width="*"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Apply" Width="90" IsDefault="True" Click="Apply_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="90" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
