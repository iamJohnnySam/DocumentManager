<Window x:Class="DocumentManager.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About — LaTeX Document Manager" Height="300" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="LaTeX Document Manager (LDMS)"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,12"
                   Text="A desktop application for managing LaTeX documents with shared sections, versioned revisions, and integrated compilation. Designed to streamline multi-document LaTeX workflows with reusable components."/>

        <TextBlock Grid.Row="2" FontSize="13" Margin="0,0,0,4">
            <Run Text="Developed by " />
            <Run Text="Johnny Samarasinghe" FontWeight="SemiBold"/>
        </TextBlock>

        <TextBlock Grid.Row="3" FontSize="13" Margin="0,0,0,12">
            <Run Text="Source Code: "/>
            <Hyperlink x:Name="GitHubLink" Click="GitHubLink_Click">
                https://github.com/iamJohnnySam/DocumentManager
            </Hyperlink>
        </TextBlock>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" IsDefault="True" IsCancel="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
