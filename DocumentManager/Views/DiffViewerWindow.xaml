<Window x:Class="DocumentManager.Views.DiffViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revision Comparison" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Old Version" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox x:Name="OldTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" AcceptsReturn="True"
                         FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap"/>
            </DockPanel>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="Gray"/>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Text="New Version" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox x:Name="NewTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" AcceptsReturn="True"
                         FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap"/>
            </DockPanel>
        </Grid>
    </Grid>
</Window>
